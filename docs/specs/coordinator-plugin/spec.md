---
status: building
created: 2026-08-17
---

# Coordinator plugin

The Coordinator is a plugin-owned workspace agent with one hidden managed conversation per workspace. Workflow settings select monitored worksteps and define a prompt per selected workstep. Coordinator Settings owns the editable base prompt and runbook; every check composes the base prompt, the workstep prompt, and immutable safety invariants.
